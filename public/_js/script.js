if (window.SimpleSlide) {
  new SimpleSlide({
    slide: "slide", // nome do atributo
    nav: "true", // menu ativo
    time: 5000, // tempo transição
  });
}

if (window.SimpleAnime) {
  new SimpleAnime();
}
